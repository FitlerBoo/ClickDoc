<Window x:Class="ClickDoc.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        d:DataContext="{d:DesignInstance vm:MainWindowVM}"
        xmlns:vm="clr-namespace:ClickDoc.ViewModels"
        xmlns:local="clr-namespace:ClickDoc"
        mc:Ignorable="d"
        Title="ClickDoc" Height="800" Width="800">

    <Grid Background="#FF8B73">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        
        <Button Style="{StaticResource CreateButtonStyle}"
                Height="200" Width="160"
                Command="{Binding CreateCommand}"/>
        
        <Button Style="{StaticResource DowloadButtonStyle}"
                Height="200" Width="160"
                Grid.Column="1"
                Command="{Binding LoadDocumentCommand}"/>
    </Grid>
</Window>
